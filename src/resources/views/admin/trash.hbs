<div class="mt-4">
  <div class="container">
    <h1>Thùng rác</h1>
    <a href="/admin">Quay lại</a>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Title</th>
          <th scope="col">Level</th>
          <th scope="col">CreateAt</th>
          <th scope="col"></th>
        </tr>
      </thead>
      {{#each courses}}
        <tbody>
          <tr>
            <th>{{sum @index}}</th>
            <td>{{this.title}}</td>
            <td>{{this.level}}</td>
            <td>{{this.createdAt}}</td>
            <td>
              <a
                href=""
                type="button"
                class="btn btn-link"
                data-toggle="modal"
                data-target="#deleteModal"
                data-id="{{this._id}}"
              >Xóa vĩnh viễn</a>
              <a
                href=""
                type="button"
                class="btn btn-link"
                id="restoreBtn"
                data-id="{{this._id}}"
              >Khôi phục</a>
            </td>
          </tr>
        </tbody>
            {{else}}
                <tr>
                  <td colspan="4" class="text-center">Thùng rác đang trống.</a> </td>
                </tr>
      {{/each}}
    </table>
  </div>
</div>

{{! modal }}

<div
  class="modal fade"
  id="deleteModal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Xóa vĩnh viễn khóa học</h5>
        <button
          type="button"
          class="close"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Xóa vĩnh viễn sẽ không thể khôi phục. Bạn có chắc chắn muốn xóa vĩnh viễn khóa học này?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-danger" id="deleteBtn">Xóa vĩnh viễn</button>
        <button
          type="button"
          class="btn btn-secondary"
          data-dismiss="modal"
        >Hủy</button>
      </div>
    </div>
  </div>
</div>

<form name="delete-form-modal" method="POST"></form>
<form name="restore-form-modal" method="POST"></form>

<script>
  var courseId;
  const deleteForm = document.forms['delete-form-modal'];
  const deleteBtn = document.querySelector("#deleteBtn");

    const restoreForm = document.forms['restore-form-modal'];
  const restoreBtn = document.querySelector("#restoreBtn");
  



  document.addEventListener('DOMContentLoaded', function() {
  $('#deleteModal').on('show.bs.modal', function (event) { 
    var button = $(event.relatedTarget);
    courseId = button.data('id');
    }); 
  }); 

 
  deleteBtn.addEventListener("click", function(){ 
    deleteForm.action = '/admin/' + courseId + '/force?_method=DELETE';
    deleteForm.submit();  
  })

  restoreBtn.addEventListener("click", function(e){
    e.preventDefault();
    var courseId = $(this).data('id');
    restoreForm.action = '/admin/' + courseId + '/restore?_method=PATCH';
    restoreForm.submit();
  })
</script>